# Memory Mirror

Everything written to the agent's project memory is mirrored here automatically,
newest at the bottom, timestamped and attributed to its source file. Maintained by
the `memory_mirror.py` hook.
