# Action items

## Open

- [ ] **Review the pilot** — 3 transformed lessons in the local preview. Approve the
      style (simplification depth, example tone, image treatment) before rollout.
- [ ] **Roll out to remaining lessons** — after sign-off, apply the same pass to the
      other ~29 course lessons + guides. One diagram/photo/screenshot per lesson where it
      earns its place; not every lesson needs an image.
- [ ] **Push** — when ready, run the project push:
      `git push origin feature/content-simplify-images` then open a PR (never push to `main`).
